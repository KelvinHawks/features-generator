export default function Navbar(){
    return(
        <nav className="Nav--bar">
            <img src="./images/geo-Logo.avif" alt="football"/>
            <p>Meme generator</p>
        </nav>
    )
}